{
  "name": "kosen-grade-planner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "scrape": "node scripts/scrape-syllabus.mjs",
    "test": "node --test",
    "build": "vite build",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
